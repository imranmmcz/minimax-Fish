# ফিশ কেয়ার সিস্টেম - দ্রুত শুরু গাইড

## ⚡ ৫ মিনিটে ইনস্টলেশন

### ধাপ ১: প্রয়োজনীয় সফটওয়্যার ইনস্টল করুন

#### XAMPP (Windows/Mac/Linux) - সুপারিশকৃত
1. [XAMPP ডাউনলোড করুন](https://www.apachefriends.org/)
2. ইনস্টল করুন (Apache এবং MySQL সিলেক্ট করুন)
3. XAMPP Control Panel চালু করুন
4. Apache এবং MySQL start করুন

#### অথবা WAMP (Windows)
1. [WAMP ডাউনলোড করুন](https://www.wampserver.com/)
2. ইনস্টল করুন এবং চালু করুন

### ধাপ ২: প্রজেক্ট সেটআপ

1. **ফোল্ডার কপি করুন:**
   ```
   fishcare ফোল্ডারটি কপি করে এখানে রাখুন:
   
   XAMPP: C:\xampp\htdocs\fishcare
   WAMP: C:\wamp64\www\fishcare
   Linux: /var/www/html/fishcare
   ```

2. **ডেটাবেস তৈরি করুন:**
   - ব্রাউজারে যান: `http://localhost/phpmyadmin`
   - "New" বাটনে ক্লিক করুন
   - Database name: `fishcare`
   - Collation: `utf8mb4_unicode_ci`
   - "Create" বাটনে ক্লিক করুন

3. **SQL ইম্পোর্ট করুন:**
   - `fishcare` ডেটাবেস সিলেক্ট করুন
   - "Import" ট্যাবে যান
   - "Choose File" ক্লিক করে `database/fishcare.sql` সিলেক্ট করুন
   - "Go" বাটনে ক্লিক করুন
   - সফল মেসেজ দেখুন

4. **ডেটাবেস কনফিগ করুন:**
   `config/database.php` ফাইল ওপেন করুন এবং প্রয়োজনে এডিট করুন:
   ```php
   define('DB_HOST', 'localhost');  // সাধারণত পরিবর্তন করতে হয় না
   define('DB_USER', 'root');       // XAMPP default
   define('DB_PASS', '');           // XAMPP তে খালি
   define('DB_NAME', 'fishcare');   // একই রাখুন
   ```

### ধাপ ৩: ওয়েবসাইট চালু করুন

ব্রাউজারে যান:
```
http://localhost/fishcare
```

✅ যদি হোম পেজ দেখেন, তাহলে সফল!

### ধাপ ৪: লগইন করুন

**অ্যাডমিন অ্যাকাউন্ট:**
```
URL: http://localhost/fishcare/pages/auth/login.php
Username: admin
Password: admin123
```

**অন্যান্য ডেমো অ্যাকাউন্ট:**
- চাষী: `farmer1` / `farmer123`
- হোলসেলার: `wholesaler1` / `wholesaler123`

---

## 🚨 সাধারণ সমস্যা এবং সমাধান

### সমস্যা ১: "Database connection failed"

**সমাধান:**
1. MySQL চালু আছে কিনা দেখুন (XAMPP Control Panel)
2. `config/database.php` এ credentials চেক করুন
3. ডেটাবেস `fishcare` তৈরি করেছেন কিনা নিশ্চিত করুন

### সমস্যা ২: Blank/White page

**সমাধান:**
1. PHP error log চেক করুন:
   - XAMPP: `C:\xampp\apache\logs\error.log`
2. Browser console চেক করুন (F12)
3. `config/config.php` এ error reporting চালু করুন:
   ```php
   error_reporting(E_ALL);
   ini_set('display_errors', 1);
   ```

### সমস্যা ৩: CSS/Images লোড হচ্ছে না

**সমাধান:**
1. ফাইল পাথ চেক করুন - সব পাথ `/fishcare/` দিয়ে শুরু হয়
2. Browser cache ক্লিয়ার করুন (Ctrl+Shift+Delete)
3. Browser DevTools (F12) এ Network tab চেক করুন

### সমস্যা ৪: "Permission denied" file upload error

**সমাধান:**
Windows:
- `assets/uploads/` ফোল্ডারে right-click
- Properties → Security → Edit
- "Full control" চেক করুন

Linux/Mac:
```bash
chmod -R 755 assets/uploads/
```

---

## 📱 মোবাইল থেকে টেস্ট করুন

1. আপনার কম্পিউটারের IP address জানুন:
   - Windows: `ipconfig` (Command Prompt)
   - Linux/Mac: `ifconfig`
   
2. মোবাইল এবং কম্পিউটার একই WiFi নেটওয়ার্কে থাকতে হবে

3. মোবাইল ব্রাউজারে যান:
   ```
   http://YOUR_IP_ADDRESS/fishcare
   উদাহরণ: http://192.168.1.100/fishcare
   ```

---

## 🔧 প্রোডাকশন ডিপ্লয়মেন্ট

### লাইভ সার্ভারে আপলোড করার আগে:

1. **সিকিউরিটি:**
   - সব ডিফল্ট পাসওয়ার্ড পরিবর্তন করুন
   - `config/config.php` এ error display বন্ধ করুন:
     ```php
     error_reporting(0);
     ini_set('display_errors', 0);
     ```

2. **ডেটাবেস:**
   - cPanel/Plesk এ নতুন database তৈরি করুন
   - `config/database.php` আপডেট করুন
   - SQL ফাইল ইম্পোর্ট করুন

3. **ফাইল পারমিশন:**
   ```bash
   chmod 644 *.php
   chmod 755 assets/uploads/
   chmod 644 .htaccess
   ```

4. **HTTPS সেটআপ:**
   - `.htaccess` এ HTTPS redirect uncomment করুন
   - SSL certificate ইনস্টল করুন (Let's Encrypt free)

5. **পারফরম্যান্স:**
   - PHP OPcache enable করুন
   - Gzip compression enable করুন
   - CDN সেটআপ করুন (optional)

---

## 📞 সাপোর্ট

এই গাইড অনুসরণ করেও সমস্যা হলে:

1. `README.md` ফাইল পুরো পড়ুন
2. Error logs চেক করুন
3. প্রতিটি ধাপ আবার চেষ্টা করুন

---

## ✅ চেকলিস্ট

ইনস্টলেশন সম্পন্ন করতে:

- [ ] XAMPP/WAMP ইনস্টল হয়েছে
- [ ] Apache এবং MySQL চালু আছে
- [ ] `fishcare` ডেটাবেস তৈরি হয়েছে
- [ ] SQL ফাইল ইম্পোর্ট সফল
- [ ] `config/database.php` সঠিক
- [ ] `http://localhost/fishcare` ওপেন হচ্ছে
- [ ] লগইন কাজ করছে
- [ ] ড্যাশবোর্ড দেখা যাচ্ছে

সব চেকমার্ক দিলে আপনি প্রস্তুত! 🎉

---

**শুভকামনা!** ফিশ কেয়ার সিস্টেম ব্যবহার করে আপনার মৎস্য ব্যবসা ডিজিটাইজ করুন।
