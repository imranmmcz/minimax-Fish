# ফিশ কেয়ার সিস্টেম - Complete PHP/MySQL Website

একটি সম্পূর্ণ, কার্যকরী এবং সিকিউর ওয়েবসাইট সিস্টেম যা বাংলাদেশের মৎস্য চাষ ব্যবসায়ের জন্য 6টি ইউজার রোলের জন্য আলাদা ড্যাশবোর্ড প্রদান করে।

## বৈশিষ্ট্য সমূহ

### ✅ মাল্টি-রোল সিস্টেম
- **অ্যাডমিন:** সম্পূর্ণ সিস্টেম নিয়ন্ত্রণ
- **চাষী:** পুকুর ব্যবস্থাপনা, আয়-ব্যয় ট্র্যাকিং
- **হোলসেলার:** ইনভয়েস ও শিপমেন্ট ম্যানেজমেন্ট
- **বিক্রেতা:** দৈনন্দিন বিক্রয় ও স্টক পরিচালনা
- **ক্রেতা:** অর্ডার ট্র্যাকিং
- **ভিজিটর:** পাবলিক তথ্য অ্যাক্সেস

### ✅ Glassmorphism ডিজাইন থিম
- আধুনিক এবং আকর্ষণীয় UI
- জলজ থিম (নীল-সবুজ কালার স্কিম)
- রেসপন্সিভ ডিজাইন (মোবাইল সাপোর্ট)

### ✅ সিকিউরিটি
- Password hashing (bcrypt)
- CSRF protection
- SQL injection prevention (PDO prepared statements)
- XSS prevention
- Session management

### ✅ পূর্ণ বাংলা ভাষা সাপোর্ট
- সম্পূর্ণ ইন্টারফেস বাংলায়
- বাংলা ফন্ট (Hind Siliguri)

## প্রযুক্তি স্ট্যাক

- **Backend:** PHP 7.4+ (PDO)
- **Database:** MySQL 5.7+
- **Frontend:** HTML5, CSS3 (Glassmorphism), JavaScript (ES6+)
- **Fonts:** Google Fonts (Hind Siliguri, Poppins)

## সিস্টেম প্রয়োজনীয়তা

- PHP 7.4 অথবা তার উপরে
- MySQL 5.7 অথবা তার উপরে
- Apache/Nginx web server
- mod_rewrite enabled (Apache এর জন্য)

## ইনস্টলেশন গাইড

### ১. প্রজেক্ট ডাউনলোড

```bash
# প্রজেক্ট ফোল্ডার আপনার web server এর root directory তে কপি করুন
# Example: /var/www/html/fishcare বা C:\xampp\htdocs\fishcare
```

### ২. ডেটাবেস সেটআপ

```bash
# MySQL এ লগইন করুন
mysql -u root -p

# ডেটাবেস SQL ফাইল ইম্পোর্ট করুন
mysql -u root -p < database/fishcare.sql
```

অথবা phpMyAdmin ব্যবহার করে:
1. phpMyAdmin ওপেন করুন
2. "Import" ট্যাবে যান
3. `database/fishcare.sql` ফাইল সিলেক্ট করুন
4. "Go" বাটনে ক্লিক করুন

### ৩. ডেটাবেস কনফিগারেশন

`config/database.php` ফাইল এডিট করুন:

```php
define('DB_HOST', 'localhost');    // আপনার ডেটাবেস হোস্ট
define('DB_USER', 'root');         // আপনার ডেটাবেস ইউজারনেম
define('DB_PASS', '');             // আপনার ডেটাবেস পাসওয়ার্ড
define('DB_NAME', 'fishcare');     // ডেটাবেস নাম
```

### ৪. আপলোড ডিরেক্টরি পারমিশন

```bash
# Linux/Mac:
chmod -R 755 assets/uploads/

# Windows: ফোল্ডারে লিখার পারমিশন দিন
```

### ৫. ওয়েবসাইট অ্যাক্সেস

ব্রাউজারে যান:
```
http://localhost/fishcare
```

## ডিফল্ট লগইন তথ্য

### অ্যাডমিন অ্যাকাউন্ট
- **Username:** admin
- **Password:** admin123
- **Email:** admin@fishcare.com

### চাষী অ্যাকাউন্ট (ডেমো)
- **Username:** farmer1
- **Password:** farmer123
- **Email:** farmer@example.com

### হোলসেলার অ্যাকাউন্ট (ডেমো)
- **Username:** wholesaler1
- **Password:** wholesaler123
- **Email:** wholesaler@example.com

⚠️ **নিরাপত্তার জন্য প্রথম লগইনের পর ডিফল্ট পাসওয়ার্ড পরিবর্তন করুন!**

## ফোল্ডার স্ট্রাকচার

```
fishcare/
├── config/                 # কনফিগারেশন ফাইল
│   ├── database.php       # ডেটাবেস কানেকশন
│   └── config.php         # সাধারণ সেটিংস
├── includes/              # শেয়ারড PHP ফাইল
│   ├── header.php         # হেডার টেমপ্লেট
│   ├── footer.php         # ফুটার টেমপ্লেট
│   ├── auth.php           # Authentication ফাংশন
│   └── functions.php      # হেল্পার ফাংশন
├── api/                   # API endpoints (AJAX)
│   ├── auth.php
│   ├── user.php
│   ├── pond.php
│   ├── income_expense.php
│   ├── invoice.php
│   └── report.php
├── pages/                 # পেজ ফাইল
│   ├── auth/             # লগইন/রেজিস্টার
│   ├── public/           # পাবলিক পেজ
│   └── dashboard/        # ড্যাশবোর্ড (রোল ভিত্তিক)
│       ├── farmer/
│       ├── wholesaler/
│       ├── seller/
│       ├── customer/
│       └── admin/
├── assets/               # স্ট্যাটিক ফাইল
│   ├── css/
│   │   ├── style.css           # মূল স্টাইল
│   │   ├── components.css      # UI কম্পোনেন্ট
│   │   └── responsive.css      # রেসপন্সিভ ডিজাইন
│   ├── js/
│   │   ├── script.js           # মূল JavaScript
│   │   └── charts.js           # চার্ট functionality
│   ├── images/
│   └── uploads/                # ইউজার আপলোড ফাইল
├── database/
│   └── fishcare.sql           # ডেটাবেস স্কিমা + স্যাম্পল ডেটা
└── index.php                   # হোম পেজ
```

## ডেটাবেস স্ট্রাকচার

মূল টেবিল সমূহ:

1. **users** - ইউজার অ্যাকাউন্ট (6 রোল)
2. **divisions, districts, upazilas** - বাংলাদেশ লোকেশন ডেটা
3. **fish_species** - মাছের প্রজাতি
4. **fish_diseases** - মাছের রোগ
5. **ponds** - পুকুর তথ্য (চাষীদের জন্য)
6. **income_expenses** - আয়-ব্যয় রেকর্ড
7. **invoices, invoice_items** - ইনভয়েস সিস্টেম
8. **shipments** - শিপমেন্ট ট্র্যাকিং
9. **sales** - বিক্রয় রেকর্ড
10. **stocks** - স্টক ম্যানেজমেন্ট
11. **market_prices** - বাজার মূল্য
12. **expert_questions, expert_answers** - প্রশ্ন-উত্তর ফোরাম
13. **system_logs** - সিস্টেম অ্যাক্টিভিটি লগ

## মূল ফিচার

### চাষী ড্যাশবোর্ড
- পুকুর যোগ/সম্পাদনা/ডিলিট
- আয় রেকর্ড করুন
- ব্যয় রেকর্ড করুন
- লেনদেন তালিকা দেখুন
- আর্থিক রিপোর্ট
- ড্যাশবোর্ড চার্ট

### হোলসেলার ড্যাশবোর্ড
- ইনভয়েস তৈরি/প্রিন্ট
- শিপমেন্ট ম্যানেজমেন্ট
- বকেয়া ট্র্যাকিং
- স্টক ম্যানেজমেন্ট

### বিক্রেতা ড্যাশবোর্ড
- দ্রুত বিক্রয় এন্ট্রি
- দৈনিক বিক্রয় রিপোর্ট
- স্টক সতর্কতা
- গ্রাহক ম্যানেজমেন্ট

### অ্যাডমিন ড্যাশবোর্ড
- ইউজার ম্যানেজমেন্ট
- মাস্টার ডেটা পরিচালনা
- সিস্টেম কনফিগারেশন
- সিস্টেম লগ

### পাবলিক পেজ (লগইন ছাড়া)
- টুলসেবা
- মৎস্য চাষ পরামর্শ
- রোগ ব্যবস্থাপনা
- বাজার দর
- ফিশ ক্যালকুলেটর
- প্রজাতির তথ্য ভান্ডার
- বিশেষজ্ঞের জিজ্ঞাসা (ফোরাম)

## নিরাপত্তা বৈশিষ্ট্য

- ✅ Password hashing (bcrypt)
- ✅ CSRF token protection
- ✅ SQL injection prevention (PDO prepared statements)
- ✅ XSS prevention (output escaping)
- ✅ Session hijacking prevention
- ✅ Input validation & sanitization
- ✅ Role-based access control
- ✅ Activity logging

## কাস্টমাইজেশন

### রঙ পরিবর্তন করুন

`assets/css/style.css` ফাইলের `:root` সেকশনে CSS variables এডিট করুন:

```css
:root {
    --color-primary-500: #00BCD4;  /* আপনার পছন্দের রঙ */
    --color-secondary-500: #4CAF50;
    /* ... */
}
```

### লোগো পরিবর্তন করুন

`includes/header.php` ফাইলে লোগো SVG এডিট করুন।

### নতুন রোল যোগ করুন

1. `database/fishcare.sql` এ `users` টেবিলের `role` ENUM আপডেট করুন
2. `config/config.php` এ `$ROLE_DASHBOARD` array আপডেট করুন
3. নতুন ড্যাশবোর্ড পেজ তৈরি করুন

## ট্রাবলশুটিং

### "Access Denied" Error
- ডেটাবেস credentials চেক করুন (`config/database.php`)
- MySQL সার্ভার চালু আছে কিনা দেখুন

### "File Upload Failed"
- `assets/uploads/` ফোল্ডারে লিখার পারমিশন দিন

### Blank Page / White Screen
- PHP error logging চালু করুন
- `config/config.php` এ error_reporting চেক করুন
- Server error logs দেখুন

### CSS/JS লোড হচ্ছে না
- ফাইল পাথ সঠিক আছে কিনা চেক করুন
- Browser cache ক্লিয়ার করুন

## পারফরম্যান্স অপটিমাইজেশন

- Database indexing ইতিমধ্যে configured
- CSS/JS minification প্রয়োজনে করুন
- Image optimization করুন
- PHP OPcache enable করুন
- Browser caching সেটআপ করুন

## ব্রাউজার সাপোর্ট

- ✅ Chrome (latest)
- ✅ Firefox (latest)
- ✅ Safari (latest)
- ✅ Edge (latest)
- ⚠️ IE 11 (partial support)

## লাইসেন্স

এই প্রজেক্টটি শিক্ষা ও ব্যবসায়িক উদ্দেশ্যে ফ্রি ব্যবহার করা যাবে।

## সাপোর্ট

সমস্যার সম্মুখীন হলে:
1. এই README আবার পড়ুন
2. Error logs চেক করুন
3. Database connection verify করুন

## ভবিষ্যৎ আপডেট

- 📊 Advanced reporting system
- 📱 Mobile app integration
- 🔔 Push notifications
- 💳 Payment gateway integration
- 📧 Email notifications
- 📄 PDF export functionality

---

**তৈরি করেছে:** MiniMax Agent  
**সংস্করণ:** 1.0.0  
**তারিখ:** ২০২৫-১১-০৩
